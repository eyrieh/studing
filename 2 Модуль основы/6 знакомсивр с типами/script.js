let number = 5.3; //число там
const one = 1;

console.log(4/0)

let maxim = 1
let chel = maxim //если скобок нет и нет переменной = смысла не имеет, но если скобки есть = строка (имеет смысол)
let bool = (false) //булиновое значение//

console.log(alex) //если мы пытаемся выбрать переменную, которой нет, но выдаст сообщениме is not defind

let box;
console.log(box) //если мы не задаем значение переменной, то выдаст сообщение underfind

const nigger = {
   owner: 'chel',
   age: 99,
   black: true       //<------объект {}
}
// console.log(nigger.age)

let white = nigger.age //white - переменная со значением nigger.age
console.log(white) //nigger.age - свойство объекта
let snow =['snejinka', 'snejok', 'grad']; //<---- MASSIVE ARRAY
console.log(snow[1])// тут ставятся квадратные скобки